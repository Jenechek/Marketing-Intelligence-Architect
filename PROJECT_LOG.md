# PROJECT_LOG.md — Журнал выполнения проекта

## Назначение

Файл фиксирует успешно завершённые задачи и следующий утверждённый шаг.

Он не заменяет:

- `TASKS.md` — текущий план;
- `ROADMAP.md` — последовательность этапов;
- `CHANGELOG.md` — заметные изменения продукта.

## Текущий статус

**Текущий этап:** Этап 2. Первый обход сайта — в работе

**Последняя завершённая задача:** TASK-0010 — ручной запуск полного обхода и прогресс

**Текущая задача:** TASK-0011 — расширенные настройки полного обхода, подготовлена к приёмке

**Следующий шаг:** провести пользовательскую приёмку TASK-0011; TASK-0012 не начинать

## Обязательное правило после завершения задачи

После каждой успешно выполненной и проверенной задачи необходимо:

1. обновить `TASKS.md`;
2. обновить `PROJECT_LOG.md`;
3. при необходимости обновить `ROADMAP.md`;
4. при необходимости обновить `CHANGELOG.md`;
5. если принято новое решение — обновить `DECISIONS.md`;
6. если изменились правила проекта — обновить `PROJECT.md`.

Задача не считается завершённой, пока обязательные документы не приведены в актуальное состояние.

## Запрет на пропуск задач

Задачи выполняются только в утверждённой последовательности.

Нельзя:

- начинать следующую задачу до завершения и проверки текущей;
- пропускать незавершённую задачу ради более поздней;
- отмечать задачу завершённой без проверки результата;
- менять порядок задач без явного обновления `TASKS.md` и, при необходимости, `ROADMAP.md`.

Если задача заблокирована, блокировка фиксируется в `TASKS.md`. После этого план изменяется только отдельным явным решением.

## Формат записи

### TASK-XXXX — Название задачи

**Дата завершения:** YYYY-MM-DD  
**Статус:** завершено

**Что выполнено:**

- результат 1;
- результат 2.

**Что проверено:**

- проверка 1;
- проверка 2.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`;
- другие документы при необходимости.

**Следующая задача:**

TASK-XXXX — название следующей задачи.

---

## История

### TASK-0010 — Ручной запуск полного обхода и прогресс

**Дата завершения:** 2026-07-15

**Статус:** завершено

**Что выполнено:**

- добавлены отдельные экраны запуска и текущего состояния полного обхода;
- защищённый POST создаёт запуск до перенаправления и запускает отслеживаемую фоновую задачу;
- текущие счётчики сохраняются без преждевременной публикации записей страниц;
- повторный запуск ведёт к активному run без новой записи и сетевого обращения;
- удаление сайта во время его активного обхода блокируется, предотвращая повторное использование ID и смешивание данных;
- shutdown и последующий startup безопасно переводят незавершённый запуск в `interrupted`.

**Что проверено:**

- на точном HEAD `cd84da9071233267114cb05c591b44efe2252b64` независимо прошли 77 автоматических тестов;
- реальный loopback-сайт подтвердил живой прогресс, доступность интерфейса, защиту повторного запуска и удаления, завершение и последующее подтверждённое удаление;
- запросы выполнены в ожидаемом порядке: `/robots.txt`, `/`, `/a`, `/b`, `/c`.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`;
- `README.md`;
- `CHANGELOG.md`.

**Следующая задача:**

Определить и согласовать TASK-0011; реализацию не начинать до явного согласования.

---

### TASK-0009 — Сохранение запусков обхода и безопасное удаление связанных данных

**Дата завершения:** 2026-07-15

**Статус:** завершено

**Что выполнено:**

- добавлены модели запуска и упорядоченной записи страницы без HTML, текста и найденных ссылок;
- запуск фиксируется до crawler, завершение и страницы сохраняются одной транзакцией, неожиданная ошибка записывает `failed` и не скрывается;
- startup переводит оставшиеся `running` в `interrupted`, а SQLite сохраняет результаты после пересоздания engine;
- подтверждённое удаление одной транзакцией удаляет проверки, страницы, запуски и выбранный сайт; отдельные количества показаны до подтверждения;
- автоматический срок хранения, отдельное управление runs, новые экраны, фоновые задачи, снимки и зависимости не добавлены.

**Что проверено:**

- на точном HEAD `8c0422800177209f81daedb3630480e8336b378b` прошли 70 автоматических тестов, `compileall` и `git diff --check`;
- независимая приёмка по поручению пользователя подтвердила количества `1/0/0`, безопасность отмены, удаление сайта вместе с историей и отсутствие удалённого сайта после перезапуска;
- предыдущая фактическая проверка с двумя сайтами подтвердила, что удаление выбранного сайта сохраняет второй.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`;
- `DECISIONS.md` (DEC-020).

**Следующий шаг:**

Определить и согласовать TASK-0010; реализацию не начинать до явного согласования.

---

### TASK-0008 — Ядро ограниченного обхода внутренних страниц

**Дата завершения:** 2026-07-15

**Статус:** завершено

**Что выполнено:**

- создано последовательное BFS-ядро с глубиной стартовой страницы 0 и безопасными настройками по умолчанию;
- каждый уникальный URL учитывается в лимите, а нормализация оставляет только exact same origin HTTP(S)-адреса;
- `robots.txt` загружается один раз и применяется к каждой странице; запрещённые URL и перенаправления не запрашиваются;
- ошибки отдельных страниц не останавливают остальные, HTML читается потоково до 2 МиБ;
- результаты остаются в памяти; новые таблицы, история обходов, UI и фоновые задачи не добавлены;
- существующая проверка использует ядро в режиме одной страницы без изменения UI и истории.

**Что проверено:**

- пользовательская приёмка пройдена: сохранены результат «Доступно», две найденные ссылки, закрытый по умолчанию список и прежняя история; тестовый сервер получил только `/robots.txt` и `/`;
- отдельный loopback-сервер подтвердил BFS-запросы без запрещённых, внешних, перенаправленных и слишком глубоких URL;
- прошли 62 автоматических теста, `pip check`, `compileall` и `git diff --check`.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`.

**Следующая задача:**

Определить и согласовать TASK-0009. До сохранения запусков обхода отдельно определить их судьбу при окончательном удалении сайта; реализация следующей задачи не начата.

---

### TASK-0007 — Обнаружение внутренних ссылок стартовой страницы

**Дата завершения:** 2026-07-15

**Статус:** завершено

**Что выполнено:**

- успешная HTML-страница потоково читается в пределах 2 МиБ;
- стандартный `HTMLParser` извлекает только `<a href>`, после чего остаются нормализованные уникальные HTTP(S)-ссылки того же origin;
- до 200 ссылок показываются в раскрываемом блоке, закрытом по умолчанию, без перехода по ним;
- HTML и ссылки не сохраняются; для не-HTML, превышения размера и ошибки разбора выводятся понятные сообщения.

**Что проверено:**

- пользовательская приёмка пройдена на отдельном локальном HTTP-сервере: обнаружены две ссылки, список раскрывается вручную, автоматических переходов и ссылок в истории нет;
- ответ HTTP 503 от `robots.txt` корректно откладывает проверку и не показывает блок ссылок;
- прошли 52 автоматических теста, `pip check`, `compileall`, `git diff --check` и фактическая локальная проверка.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`;
- `README.md`;
- `CHANGELOG.md`.

**Следующая задача:**

Определить и согласовать TASK-0008 в рамках этапа 2. Реализация следующей задачи не начата.

---

### TASK-0006 — История проверок доступности

**Дата завершения:** 2026-07-15

**Статус:** завершено

**Что выполнено:**

- каждая подтверждённая ручная проверка сохраняет в SQLite время начала и завершения, статус, сообщение и доступные HTTP-коды;
- экран проверки показывает сохраняемую историю от новой записи к старой с раскрываемыми подробностями;
- подтверждение удаления показывает число связанных записей;
- сайт и его история удаляются одной транзакцией, данные других сайтов сохраняются;
- правила сети TASK-0005, содержимое страниц, снимки, настройки, архив и восстановление не изменены и не добавлены.

**Что проверено:**

- пользовательская приёмка пройдена: подтверждены сохранение истории после перезапуска, корректное локальное время, безопасная отмена удаления, совместное удаление сайта с его историей и сохранность второго сайта;
- сохранение успешных результатов и сетевых ошибок, порядок истории и перезапуск с той же SQLite;
- GET, неверные токены и неизвестный сайт не запускают сеть и не создают историю;
- отмена и неподтверждённое удаление сохраняют сайт и историю, подтверждённое удаление сохраняет данные других сайтов;
- прошли 39 автоматических тестов, `pip check`, `compileall`, `git diff --check` и фактическая проверка с временной SQLite.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`;
- `README.md`;
- `CHANGELOG.md`;
- `DECISIONS.md` (DEC-019).

**Следующая задача:**

Определить и согласовать TASK-0007 в рамках этапа 2. Реализация следующей задачи не начата.

---

### TASK-0005 — Безопасная ручная проверка доступности сайта

**Дата завершения:** 2026-07-14

**Статус:** завершено

**Что выполнено:**

- в списке сайтов добавлено вторичное действие и отдельный экран с одним основным действием запуска;
- POST защищён HMAC-токеном, связанным с ID сайта и действием проверки;
- `robots.txt` и одна стартовая страница проверяются с утверждёнными User-Agent, таймаутом, задержкой и запретом автоматических перенаправлений;
- стартовая страница запрашивается потоково без чтения и сохранения тела;
- результаты и история проверок не сохраняются в SQLite;
- одновременно выполняется не более одной проверки.

**Что проверено:**

- пользовательская приёмка пройдена;
- `https://example.com` показал корректный результат «Доступно»;
- HTTP 503 от `robots.txt` показал «Проверка отложена» и исключил запрос стартовой страницы;
- после перезапуска сайты сохранились, а предыдущий результат проверки не сохранился;
- прошли 34 автоматических теста, `pip check`, `compileall` и `git diff --check`.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`;
- `README.md`;
- `CHANGELOG.md`.

**Следующая задача:**

Определить и согласовать следующую небольшую задачу этапа 2 и правила будущей сохраняемой истории. Реализация не начата.

---

### TASK-0004 — Подтверждаемое окончательное удаление сайта

**Дата завершения:** 2026-07-14

**Статус:** завершено; этап 1 завершён

**Что выполнено:**

- на странице редактирования добавлена визуально отделённая опасная зона;
- отдельная страница подтверждения показывает название, URL и необратимость удаления;
- окончательное удаление выполняется только подтверждённым POST-запросом;
- HMAC-токен привязан к ID сайта и защищает от прямого неподтверждённого запроса;
- отмена, GET, неверный токен и неизвестный ID не удаляют данные.

**Что проверено:**

- пользовательская приёмка пройдена;
- прямой POST без токена получил 403, а сайт сохранился;
- подтверждённое удаление затронуло только выбранный сайт;
- другой сайт сохранился после перезапуска с той же SQLite;
- прошли 16 автоматических тестов, `compileall` и `git diff --check`.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`;
- `ROADMAP.md`;
- `README.md`;
- `CHANGELOG.md`.

**Следующая задача:**

Определить и утвердить первую задачу этапа 2. Реализация не начата.

---

### TASK-0003 — Редактирование сайта

**Дата завершения:** 2026-07-14

**Статус:** завершено

**Что выполнено:**

- в строке сайта добавлено вторичное действие «Редактировать»;
- отдельная форма показывает текущие название и URL и содержит одно приоритетное действие сохранения;
- отмена и ошибки проверки не изменяют сохранённые данные;
- корректные изменения сохраняются в SQLite одной транзакцией;
- неизвестный идентификатор показывает контролируемую русскоязычную страницу.

**Что проверено:**

- пользовательская приёмка пройдена;
- изменения сохраняются после перезапуска с той же SQLite;
- некорректный URL и отмена сохраняют прежние данные;
- прошли 8 автоматических тестов, `compileall` и `git diff --check`;
- удаление сайта и правила дубликатов URL не изменялись.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`;
- `CHANGELOG.md`.

**Следующая задача:**

Определить и отдельно утвердить TASK-0004 — удаление сайта с явным подтверждением. Реализация не начата.

---

### TASK-0002 — Список сайтов и форма добавления

**Дата завершения:** 2026-07-14

**Статус:** завершено

**Что выполнено:**

- добавлен основной экран со списком сайтов и понятным пустым состоянием;
- добавлена форма с полями «Название» и «URL»;
- некорректный URL отклоняется с понятной ошибкой;
- корректные сайты сохраняются в локальной SQLite одной транзакцией.

**Что проверено:**

- пользовательская приёмка пройдена;
- сайт добавляется через интерфейс и остаётся после перезапуска с той же базой;
- диагностика первоначально пустого списка подтвердила смену пути к базе при запуске без `MI_DATA_DIR`, потери данных не было;
- прошли 3 автоматических теста, `compileall` и `git diff --check`.

**Обновлённые документы:**

- `TASKS.md`;
- `PROJECT_LOG.md`.

**Следующая задача:**

Определить и утвердить TASK-0003 в рамках этапа 1. Реализация не начата.

---

### TASK-0001 — Минимальная рабочая основа

**Дата завершения:** 2026-07-14

**Статус:** завершено

**Что выполнено:**

- создано минимальное FastAPI-приложение в архитектуре модульного монолита;
- добавлены стартовая страница, SQLite через SQLModel и базовый журнал;
- добавлены автоматический тест, зависимости и инструкция запуска на Windows;
- этап 1 и управление сайтами не начинались.

**Что проверено:**

- приложение работает в Яндекс Браузере, Google Chrome и Mozilla Firefox;
- локальная база и журнал создаются;
- `pytest` проходит с отдельным временным каталогом;
- в `docs/WINDOWS.md` добавлено решение `PermissionError` для временной папки тестов.

**Следующий шаг:**

TASK-0002 — создать экран списка сайтов и форму добавления сайта. Работа над задачей не начата.

---

### TASK-0000 — Подготовка проектной документации

**Дата завершения:** 2026-07-10  
**Статус:** завершено

**Что выполнено:**

- подготовлен комплект проектной документации;
- подготовлен и согласован `REQUIREMENTS.md`;
- требования разделены на бизнес-, функциональные и нефункциональные;
- зафиксированы решения по SQLModel, безопасному обходу, анализу, импорту и проверке гипотез;
- утверждённые Markdown-документы синхронизированы между Google Drive и GitHub;
- введён журнал выполнения проекта;
- введено обязательное обновление документов после каждой успешной задачи;
- введён запрет на переход к следующей задаче до завершения текущей.

**Что проверено:**

- правила не добавляют лишних процессов;
- используются только существующие управляющие документы проекта.
- открытые вопросы не блокируют кодовую часть этапа 0;
- `REQUIREMENTS.md` включён в обязательный набор источников проекта.

**Следующая задача:**

TASK-0001 — создать минимальную рабочую основу приложения.
